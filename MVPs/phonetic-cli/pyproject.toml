[project]
name = "phonetic-cli"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pythainlp>=5.1.2",
    "numpy>=1.26.0",
    "tltk>=1.0.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
